services:
  postgres:
    image: postgres:16-alpine
    container_name: glowcycle-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: glowcycle
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - glowcycle_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d glowcycle"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  glowcycle_pgdata:
